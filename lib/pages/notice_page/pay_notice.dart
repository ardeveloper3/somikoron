import 'package:flutter/material.dart';

class PayNotice extends StatelessWidget {
  String title;
   PayNotice({required this.title});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.lightBlueAccent,
      body: Center(
        child: Text(""),
      ),
    );
  }
}
